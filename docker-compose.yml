services:
  bot:
    build: .
    env_file: .env
    volumes:
      # The campaign database lives here -- keep this volume and your stories
      # survive image rebuilds.
      - dmk_data:/app/local
    restart: unless-stopped

volumes:
  dmk_data:
